# ============================================
# Database Configuration
# ============================================
# When using docker-compose, DATABASE_URL will be automatically constructed from the variables below
POSTGRES_USER="root"
POSTGRES_PASSWORD="mysecretpassword"
POSTGRES_DB="local"

# ============================================
# Application Configuration
# ============================================
# Node environment: "development" or "production"
NODE_ENV="development"

# Application port (default: 5173)
APP_PORT="5173"

# Production environment flag
# "1" = production mode (hide incomplete modules)
# "0" = development mode (show all modules)
# This is a PUBLIC variable, so it's accessible in client-side code
PUBLIC_PRODUCTION_ENVIRONMENT="1"
